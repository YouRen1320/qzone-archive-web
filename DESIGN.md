# 拾光册正式版设计规范：雨路

## 1. 视觉主题与氛围

拾光册不是 QQ 空间皮肤，也不是仿古杂志。正式界面是一段无人物的江南雨夜长镜头：使用者从门外进入，沿屋檐、桥洞、船舱和灯下完成一次真实的临时归档，最后在清晨把文件保存到自己的设备。

品牌关键词是克制、潮湿、靠近。摄影、留白、环境声和操作响应负责现代感，不使用水墨、宣纸、印章、圆窗或旅游景区式红灯笼表达江南。

## 2. 色彩及职责

| 名称 | 色值 | 用途 |
| --- | --- | --- |
| 雨白 | `#d7d7d2` | 夜景主文字、薄雾 |
| 湿石 | `#858985` | 次要信息、刻度 |
| 河墨 | `#171a18` | 浅景主文字、深色控制 |
| 苔影 | `#343a35` | 暗部、水面遮罩 |
| 旧木 | `#5b4032` | 主要动作 |
| 灯色 | `#d69b59` | 完成态和暖光，面积不超过一成 |
| 晨雾 | `#e8e3d8` | 清晨结果背景 |
| 危险 | `#9a4e40` | 删除、停止和错误 |

不使用蓝绿渐变、渐变文字或纯白压在纯黑上的强烈对比。场景图片承担大部分颜色，控件不与摄影争抢注意力。

## 3. 字体规则

界面字体为 `-apple-system`, `BlinkMacSystemFont`, `SF Pro Display`, `PingFang SC`, `Hiragino Sans GB`, sans-serif。中文依赖系统苹方或平台等价字体，避免额外字体下载暴露访问或拖慢首屏。

- 场景标题：`clamp(31px, 4.75vw, 64px)`，字重 510，行高 1.04。
- 正文：桌面 15px，手机 13px，中文行高 1.7。
- 数据：使用等宽数字特性。
- 章节编号只表示真实流程顺序。
- 不对中文标题使用负字距。

## 4. 组件样式

- 主按钮：旧木色实底，4px 圆角，至少 44px 高；按下缩放至 0.97，禁用态降低对比度。
- 次按钮：透明背景，以短下划线或文字颜色表达操作，不套卡片。
- 二维码：作为一张被灯照亮的纸出现，只有必要浅底和微弱投影。
- 表单选项：使用安静横线组织，不使用通用圆角卡片。
- 流程导航：六个位置点；真实已到达步骤可进入，尚未到达的步骤不能点击。
- 错误和恢复：保留在当前真实步骤内，给出下一步动作，不自动把用户送回开头。
- 照片浏览：八张江南细节图只承担归档过程的环境叙事，不冒充用户真实照片。

## 5. 布局原则

- 桌面使用横向电影构图，操作区不超过画面宽度的三分之一，随空间在左右岸交换。
- 手机使用独立纵向场景图，操作内容位于下三分之一并避开主体。
- 一个视口只完成一个动作，不显示传统顶栏、白色横条或全局卡片。
- 真实后端状态决定当前章节；滚动和视觉导航只能查看已经到达的章节。
- 交互内容始终保持在安全区内，桌面宽度下限 320px。

## 6. 深度与层级

深度由摄影前中后景、局部暗角和水滴折射构成。操作 DOM 永远位于装饰场景之上，场景层 `aria-hidden` 且不接收指针事件。除二维码纸面、照片和危险确认外，不使用通用卡片阴影。

## 7. 应做与不应做

应做：

- 每幕只表达当前真实动作。
- 让场景、文案和按钮共同说明下一步。
- 保留暂停、失败、中断和重新扫码的明确恢复路径。
- 让低性能设备、减少动态模式和强制颜色模式都能完成归档。
- 所有请求继续由 `useArchiveJob` 负责，场景层零 API 调用。

不应做：

- 不用风景包装假二维码、假进度或假下载。
- 不让视觉章节覆盖后端任务状态。
- 不写“旧时光”“属于你”“把记忆带回来”等广告式文案。
- 不使用玻璃卡片、粗侧边强调、渐变文字或 `transition: all`。
- 不自动播放声音，不让 Canvas 挡住控件。

## 8. 响应式与可访问性

- 验收宽度：1280、1366、1440、375 和 320px。
- 720px 以下使用独立手机场景图，减少连续视差并把 Canvas 限制为轻量模式。
- `prefers-reduced-motion: reduce` 时停止 RAF、折射和镜头移动，直接显示当前状态的静态场景。
- `forced-colors` 时隐藏装饰场景，使用纯色背景。
- 所有点击目标至少 44px，键盘焦点清晰；声音默认关闭。
- 真实状态更新使用 `aria-live`，不因 SSE 更新抢焦点或自动滚动。

## 9. 后续实现提示

- 场景容器：河墨底色，当前桌面或手机 WebP 全屏覆盖，装饰层 `pointer-events: none`，所有业务 DOM 位于其上。
- 主动作：旧木 `#5b4032`，4px 圆角，最小高度 44px，按下只动画 `transform` 到 0.97。
- 夜景文字：雨白 `#d7d7d2`，正文 15px、行高 1.7；清晨文字改用河墨 `#171a18`。
- 结果数据：等宽数字、无卡片网格，使用空间中的横线和留白分组。
- 恢复状态：保持船中或桥边场景，使用危险色 `#9a4e40` 的小型状态标记，并保留真实重新扫码或继续按钮。
